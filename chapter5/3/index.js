//задача 1
//const ucFirst = (str) => str.replace(str[0], str[0]?.toUpperCase());

//задача 2
// const checkSpam=(str)=>/xxx|viagra/i.test(str)

//задача 3
// const truncate = (str, maxlength) =>
//   str.length > maxlength ? str.slice(0, maxlength - 1) + "…" : str;

//задача 4
// const extractCurrencyValue=(str)=>+str.slice(1);
